mixin advanages-list(spec)
  - spec = spec || {}
  - spec.__class = spec.__class || ''

  - var items = [ { icon: '#ico-nose', text: '#nose-text'}, { icon: '#throat-pain', text: '#throat-text'}, { icon: '#fever', text: '#fever-text'}, { icon: '#stronger', text: '#stronger-text'}, { icon: '#lower', text: '#lower-text'}, { icon: '#blocked', text: '#block-text'}]

  ul.advantages-list(class=spec.__class)&attributes(attributes)
    each item in items
      li.advantages-list__item
        svg.icon.advantages-list__icon
          use(xlink:href=item.icon)

        svg.icon.advantages-list__text-icon
          use(xlink:href=item.text)


