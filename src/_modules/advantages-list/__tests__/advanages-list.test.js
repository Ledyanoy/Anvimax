'use strict';

import AdvantagesList from '../advantages-list';

describe('AdvantagesList View', function() {

  beforeEach(() => {
    this.advanagesList = new AdvantagesList();
  });

  it('Should run a few assertions', () => {
    expect(this.advanagesList);
  });

});
